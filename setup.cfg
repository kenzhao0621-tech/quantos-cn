[metadata]
name = quantos-cn
version = 4.0.0
description = QuantOS CN gateway

[options]
packages = find:
python_requires = >=3.9

[options.packages.find]
include =
    gateway*
    quant*
    integrations*
    services*
