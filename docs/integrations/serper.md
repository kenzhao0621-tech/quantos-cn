# Serper (disabled)

Google search via Serper API. Env: `SERPER_API_KEY`

PRIMARY search when enabled; fallback agent-reach Exa only if configured.

Not for stable historical facts — use docs/local knowledge first.
