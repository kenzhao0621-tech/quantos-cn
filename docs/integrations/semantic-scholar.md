# Semantic Scholar (disabled)

Optional enrichment for `citation-graph-builder`. Env: `SEMANTIC_SCHOLAR_API_KEY`

Local citation parsing works without this key. Do not fabricate graph edges when API unavailable.
