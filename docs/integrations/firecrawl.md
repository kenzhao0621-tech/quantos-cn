# Firecrawl (disabled)

Use Firecrawl CLI/MCP for structured page extraction. Requires API key from firecrawl.dev.

Env: `FIRECRAWL_API_KEY`

Route all output through `web-content-safety-gate` before agent use.

Do not enable until Phase 2 web research gate is verified.
